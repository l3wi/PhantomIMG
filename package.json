{
  "name": "PhantomIMG",
  "description": "A simple resourced balanced API for PhantomJS to enable dynamically serving of non-existent images.",
  "main": "index.js",
  "scripts": {
    "start": "micro"
  },
  "dependencies": {
    "micro": "^7.3.3",
    "phantom": "^4.0.3",
    "phantom-pool": "^1.2.0",
    "query-string": "^4.3.4",
    "url-parse": "^1.1.9",
    "validator": "^7.0.0"
  },
  "devDependencies": {
    "eslint": "^3.19.0",
    "eslint-config-barebones": "^0.1.3",
    "eslint-config-prettier": "^2.0.0",
    "prettier": "^1.3.1"
  },
  "eslintConfig": {
    "extends": [
      "prettier",
      "prettier/react",
      "barebones"
    ],
    "rules": {
      "no-unused-vars": "off"
    }
  }
}